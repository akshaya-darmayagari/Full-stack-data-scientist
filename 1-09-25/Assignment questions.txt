1)A security system checks whether the entered code is prime for extra authentication. Write a program to check if a given number is prime.

a=int(input("Enter a number: "))
c=0
for i in range(2,a):
    if(a%i==0):
        c+=1
if(c==0):
    print("Given number is prime number")
else:
    print("Given number is not a prime number") 


2)In a class test, a teacher wants to know the second highest score among students. Write a program to find the second largest number in a list.

a=list(map(int,input("Enter the list of numbers: ").split()))
a.remove(max(a))
print("Second largest number is:",max(a))


3)A fitness app counts only even-numbered steps to calculate “balanced walking.” Write a program to find the sum of even numbers from 1 to 50.

s=0
for i in range(1,50):
    if(i%2==0):
        s+=i
print("Sum of even numbers between 1 to 50 is:",s) 


4)A typing speed test wants to know how many uppercase and lowercase letters a user typed. Write a program to count uppercase and lowercase characters in a string.

a=input("Enter a string: ")
l,u=0,0
for i in a:
    if(i.islower()):
        l+=1
    elif(i.isupper()):
        u+=1
print("Number of lowercase letters:",l)
print("Number of uppercase letters:",u)


5)A financial tool simulates Fibonacci-like growth in savings. Write a program to generate the Fibonacci sequence up to n terms.

n=int(input("Enter number of terms: "))
a,b=0,1
print("Fibonacci series:")
for i in range(n):
    print(a,end=' ')
    a,b=b,a+b   


6)A word puzzle game checks if two words are anagrams (same letters but shuffled). Write a program to check if two strings are anagrams.

a=input("Enter a string: ")
b=input("Enter second string:")
a=list(a)
b=list(b)
if(sorted(a)==sorted(b)):
    print("Given two strings are anagrams")
else:
    print("Given two strings are not anagrams")


7)A science experiment needs to verify whether a number is perfect (sum of divisors = number). Write a program to check if a number is a perfect number.

n=int(input("Enter a number: "))
s=0
for i in range(1,n):
    if(n%i==0):
        s+=i
if(s==n):
    print(n,"is a perfect number")   
else:
    print(n,"is not a perfect number")


8)A school report system calculates percentage and assigns grades:
≥90 → A
≥75 → B
≥50 → C
<50 → Fail
Write a program that takes 5 subject marks, calculates percentage, and prints the grade.

a=list(map(int,input("Enter 5 subject marks: ").split()))
per=(sum(a)*100)/500
if(per>=90):
    print("Grade A")
elif(per>=75):
    print("Grade B")
elif(per>=50):
    print("Grade C")
elif(per<50):
    print("Fail")


9)A mathematics quiz app generates all prime numbers between 1 and 100. Write a program to print all prime numbers in this range.

print("Prime numbers between 1 to 100 are:")
for i in range(2,101):
    c=0
    for j in range(2,i):
        if(i%j==0):
            c+=1
    if(c==0):
        print(i,end=' ')


10)An online editor counts words in an article but doesn’t allow using split(). Write a program to count the number of words in a sentence.

a=input("Enter a string: ")
b=1
for i in a:
    if(i==' '):
        b+=1
print("Number of words in the given string is:",b)


11)A cinema gives a 20% discount if the customer is a student and 10% discount if the customer is a senior citizen (age ≥ 60).
Write a program that takes ticket price, age, and student status (yes/no) as input and prints the final ticket price.

a=int(input("Enter the price: "))
b=int(input("Enter the age: "))
stu=input("Enter student status: ")
if((stu.lower()=='yes')):
    a-=0.2*a
elif(b>=60):
    a-=0.1*a
print("The ticket price after discount is:",a)


12)An e-commerce website gives free delivery if the total cart value is more than ₹500. Otherwise, a delivery charge of ₹50 is applied.
Write a program that takes the cart total as input and prints the final amount payable.

a=int(input("Enter a cart total: "))
if(a<=500):
    a+=50
print("Total payable amount is:",a)


13)Marks Grading System
A college follows this grading system:
90–100: A
75–89: B
50–74: C
Below 50: Fail
Write a program that takes marks as input and prints the grade.

a=int(input("Enter the marks:"))
if(a>=90 and a<=100):
    print("Grade A")
elif(a>=75 and a<90):
    print("Grade B")
elif(a>=50 and a<75):
    print("Grade C")
elif(a<50):
    print("Fail")


14)Restaurant Tip Calculator
A restaurant suggests tips based on the bill:
Less than 500 → 5% tip
500–1000 → 10% tip
More than 1000 → 15% tip
Write a program that takes the bill amount and prints the tip to be added.

a=int(input("Enter a bill amount: "))
if(a<=500):
    t=a*0.05
elif(a>500 and a<=1000):
    t=a*0.1
else:
    t=a*0.15
print("Tip to be paid:",t)



15)Gym Membership Plan
A gym charges:
₹500 per month
₹2700 for 6 months (discounted)
₹5000 for 12 months (discounted)
Write a program where the user enters the membership period (months) and the program prints the cheapest option.

a=int(input("Enter a number of months: "))
s=0
while(a>0):
    if(a>=12):
        s+=5000
        a-=12
    elif(a>=6):
        s+=2700
        a-=6
    else:
        s+=500 
        a-=1
print("Total gym charges:",s)



16)A student must have at least 75% attendance to sit for the exam.
Write a program that takes total classes and classes attended as input and prints whether the student is eligible or not eligible.

totcl=int(input("Enter total classes held: "))
totatt=int(input("Enter total classes attended: "))
per=(totatt/totcl)*100
if(per>=75):
    print("He/she is allowed for exam")
else:
    print("He/she is not allowed for exam")



17)An ATM only allows withdrawals in multiples of 100.
Write a program that takes the withdrawal amount and prints whether the transaction is successful or failed.

a=int(input("Enter a money to be withdrawn: "))
if(a%100==0):
    print("Transaction is successful")
else:
    print("Transaction failed")



18)Weather Suggestion
Write a program that takes the temperature as input and gives a suggestion:
35 → "Too hot, stay indoors!"
20–35 → "Nice weather, go out!"
< 20 → "It’s cold, wear warm clothes!"

a=int(input("Enter the temperature:"))
if(a>35):
    print("Too hot, stay indoors!")
elif(a>=20 and a<=35):
    print("Nice weather, go out!")
elif(a<20):
    print("It’s cold, wear warm clothes!")



19)A library charges a fine for late book returns:
1–5 days → ₹10
6–10 days → ₹50
10 days → ₹100
Write a program that takes the number of late days as input and prints the fine amount.

a=int(input("Enter the number of late days:"))
if(a>=1 and a<=5):
    f=10
elif(a>5 and a<=10):
    f=50
elif(a>10):
    f=100
print("Fine amount:",f)



20)Electricity Bill Calculator
An electricity board charges:
First 100 units → ₹5 per unit
Next 100 units (101–200) → ₹7 per unit
Above 200 units → ₹10 per unit
Write a program that takes units consumed as input and prints the bill amount.

a = int(input("Enter the total units consumed: "))
p = 0
if a > 100:
    p += 100 * 5
    a -= 100
    if a > 100:
        p += 100 * 7
        a -= 100
        p += a * 10  
    else:
        p += a * 7
else:
    p += a * 5
print("Total payable amount is:", p)



